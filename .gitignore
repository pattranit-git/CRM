# ไม่ push ไฟล์เหล่านี้ขึ้น Git

# Environment variables (ข้อมูลสำคัญ เช่น password, database URL)
.env

# Node modules (ติดตั้งใหม่ได้ด้วย npm install)
node_modules/

# Log files
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db
